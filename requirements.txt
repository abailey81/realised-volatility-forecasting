# Core scientific stack
numpy>=1.24,<2.1
pandas>=2.0,<2.3
scipy>=1.11
scikit-learn>=1.3,<1.6
statsmodels>=0.14

# Parquet I/O for the intermediate / feature / macro caches
pyarrow>=14.0

# Macro feature download (public sources: FRED, Yahoo Finance)
fredapi>=0.5.1
yfinance>=0.2.30
requests>=2.31

# Figures and PDF report rendering
matplotlib>=3.7
reportlab>=4.0

# Parallelism, configuration, progress
joblib>=1.3
pyyaml>=6.0
tqdm>=4.66

# Testing
pytest>=7.4

# Optional: a PyTorch neural-network backend lives in
# src/models/neural_networks.py. The default backend is scikit-learn's
# MLPRegressor (see README); install torch only to use the PyTorch path.
# torch>=2.0,<2.6
